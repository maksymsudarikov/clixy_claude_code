# 🚀 DEPLOY FIX - GitHub Pages Routing

## ❌ ПРОБЛЕМА
На домене clixyspace.com:
- Не появляется PIN код при заходе на главную
- Нет кнопок Logout и Gift Card Link

## ✅ РЕШЕНИЕ

Проблема в том, что GitHub Pages не умеет работать с SPA роутингом.
Мы исправили это добавив:
1. `404.html` - перехватывает все неизвестные пути
2. Скрипт в `index.html` - восстанавливает правильный путь

### Что нужно сделать:

1. **Закоммитить изменения:**
```bash
git add .
git commit -m "fix: Add GitHub Pages SPA routing support"
git push
```

2. **Подождать деплоя** (~2-3 минуты)
   - Проверь в GitHub Actions: https://github.com/yourusername/clixy/actions

3. **Проверить работу:**
   - `https://clixyspace.com/` - должен показать PIN экран
   - `https://clixyspace.com/#/gift-card` - форма покупки
   - `https://clixyspace.com/#/shoot/123` - страница съемки

---

## 🔍 Как это работает:

### До исправления:
```
https://clixyspace.com/       ✅ работает (index.html)
https://clixyspace.com/about  ❌ 404 error (нет такого файла)
```

### После исправления:
```
https://clixyspace.com/       → 404.html → /?/       → #/       ✅ PIN экран
https://clixyspace.com/about  → 404.html → /?/about  → #/about  ✅ работает
```

GitHub Pages видит все пути как файлы, но у нас нет файла `/about`.
404.html перехватывает ошибку и конвертирует путь в hash route (#/).

---

## 📱 HashRouter vs BrowserRouter

**HashRouter** (то что используем):
- ✅ Работает на GitHub Pages без настроек
- ✅ Работает с любым хостингом
- URL: `https://clixyspace.com/#/gift-card`

**BrowserRouter** (чистые URL):
- ❌ Не работает на GitHub Pages
- URL: `https://clixyspace.com/gift-card`
- Требует настройку сервера (nginx, Apache)
