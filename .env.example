# Clixy Environment Variables

# Tenant selection (olga | generic)
VITE_TENANT=olga

# Gemini API Key (for AI assistant features)
GEMINI_API_KEY=your_gemini_api_key_here

# Admin PIN Hash (use the hash generated by running: node scripts/hashPin.cjs YOUR_PIN)
# Example: node scripts/hashPin.cjs 9634
# DO NOT commit your actual PIN hash to git - use .env file (which is gitignored)
VITE_ADMIN_PIN_HASH=$2a$10$replace_with_generated_bcrypt_hash

# Supabase Configuration (if using Supabase for backend)
# VITE_SUPABASE_URL=your_supabase_url
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
