# 🚀 Деплой Clixy на GitHub Pages

## Подготовка завершена ✅

Все файлы для деплоя уже настроены:
- ✅ `vite.config.ts` - обновлен с `base: './'`
- ✅ `package.json` - добавлен скрипт `deploy`
- ✅ `.github/workflows/deploy.yml` - настроен GitHub Actions

## Шаги для деплоя

### 1. Создайте репозиторий на GitHub

Перейдите на https://github.com/new и создайте новый **публичный** репозиторий (например, `clixy`)

**ВАЖНО:** Репозиторий должен быть **PUBLIC**, а не private!

### 2. Подключите удаленный репозиторий

```bash
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/clixy.git

# Или через SSH:
git remote add origin git@github.com:YOUR_USERNAME/clixy.git
```

### 3. Закоммитьте новые изменения

```bash
git add .
git commit -m "chore: add GitHub Pages deployment configuration"
```

### 4. Запушьте в GitHub

```bash
git push -u origin main
```

### 5. Настройте GitHub Pages

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Pages**
3. В разделе **Source** выберите:
   - Source: **GitHub Actions**
4. Сохраните настройки

### 6. Дождитесь деплоя

GitHub Actions автоматически:
- Установит зависимости
- Соберет проект
- Задеплоит на GitHub Pages

Процесс займет 2-3 минуты. Следите за прогрессом в разделе **Actions** вашего репозитория.

### 7. Откройте ваше приложение! 🎉

После успешного деплоя ваше приложение будет доступно по адресу:

```
https://YOUR_USERNAME.github.io/clixy/
```

## Автоматический деплой

После первой настройки каждый `git push` в ветку `main` будет автоматически:
1. Запускать билд
2. Деплоить на GitHub Pages
3. Обновлять живую версию

## Альтернатива: Ручной деплой

Если хотите задеплоить вручную без GitHub Actions:

```bash
# 1. Соберите проект
npm run build

# 2. Перейдите в dist
cd dist

# 3. Инициализируйте git
git init
git add -A
git commit -m 'deploy'

# 4. Запушьте в gh-pages ветку
git push -f git@github.com:YOUR_USERNAME/clixy.git main:gh-pages

cd ..
```

Затем в Settings → Pages выберите источник: **Branch: gh-pages / root**

## Проверка статуса деплоя

1. Откройте ваш репозиторий на GitHub
2. Перейдите в раздел **Actions**
3. Найдите последний workflow "Deploy to GitHub Pages"
4. Проверьте статус (должен быть зеленый ✓)

## Troubleshooting

### Деплой не работает?

1. **Проверьте, что репозиторий PUBLIC**
   - Settings → General → Danger Zone → Change visibility

2. **Проверьте настройки Pages**
   - Settings → Pages → Source должен быть "GitHub Actions"

3. **Проверьте логи Actions**
   - Actions → последний workflow → посмотрите на ошибки

4. **Проверьте permissions**
   - Settings → Actions → General → Workflow permissions
   - Выберите "Read and write permissions"

### Приложение не загружается?

1. Проверьте консоль браузера на ошибки
2. Убедитесь что `base: './'` в vite.config.ts
3. Очистите кеш браузера (Ctrl+Shift+R)

## Кастомный домен (опционально)

Чтобы использовать собственный домен:

1. Создайте файл `dist/CNAME` с вашим доменом:
   ```
   example.com
   ```

2. Настройте DNS у вашего провайдера:
   ```
   A Record: 185.199.108.153
   A Record: 185.199.109.153
   A Record: 185.199.110.153
   A Record: 185.199.111.153
   ```

3. В Settings → Pages → Custom domain введите ваш домен

---

## Готово! 🎊

Ваше приложение теперь доступно онлайн 24/7!

**Ссылка:** `https://YOUR_USERNAME.github.io/clixy/`
