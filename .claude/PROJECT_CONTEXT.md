# 🤖 AI Agent Context & Guidelines

> **Для Claude Code и других AI агентов**
> Прочитай это перед началом работы над проектом Clixy

---

## 🎯 Quick Context

**Проект:** Clixy - Production Portal для Studio Olga Prudka
**Тип:** Внутренний портал управления съемками (НЕ публичное портфолио!)
**Пользователи:**
- Команда студии (фотографы, стилисты, модели) → создают/управляют съемками
- Клиенты → смотрят детали своей съемки

**Главные файлы для чтения:**
1. `PROJECT_STATUS.md` - текущее состояние проекта
2. `CHANGELOG.md` - полная история изменений
3. `README.md` - техническая документация

---

## 📖 Protocol: Начало работы

### Шаг 1: Прочитай контекст
```bash
1. Прочитай PROJECT_STATUS.md (обязательно!)
2. Прочитай последние 3 записи в CHANGELOG.md
3. Проверь открытые TODO в PROJECT_STATUS.md
```

### Шаг 2: Пойми задачу
```bash
1. Спроси пользователя что нужно сделать
2. Уточни детали если что-то непонятно
3. Проверь не затрагивает ли задача безопасность
```

### Шаг 3: Планирование (если задача сложная)
```bash
1. Используй TodoWrite для breakdown задачи
2. Покажи план пользователю
3. Получи подтверждение перед началом
```

### Шаг 4: Выполнение
```bash
1. Делай commit'ы по мере завершения частей
2. Обновляй todo list в реальном времени
3. Тестируй изменения (npm run build)
```

### Шаг 5: Документирование
```bash
1. Обнови CHANGELOG.md с деталями изменений
2. Обнови PROJECT_STATUS.md если изменилась архитектура
3. Создай финальный commit
```

---

## 🚨 КРИТИЧЕСКИЕ ПРАВИЛА

### ⛔ НИКОГДА НЕ ДЕЛАЙ:

1. **Не ломай безопасность**
   - ❌ Убирать токены из `/shoot/:id`
   - ❌ Делать shoot pages публичными
   - ❌ Удалять PIN защиту с `/admin` или `/dashboard`
   - ❌ Коммитить `.env` файлы

2. **Не меняй без запроса**
   - ❌ Архитектуру маршрутов
   - ❌ Типы данных (Shoot, TeamMember, etc.)
   - ❌ Design system (цвета, стили)
   - ❌ Backend интеграции (Supabase, Sheets)

3. **Не удаляй**
   - ❌ Существующие features
   - ❌ Документацию
   - ❌ Тесты (если появятся)

### ✅ ВСЕГДА ДЕЛАЙ:

1. **Сохраняй безопасность**
   - ✅ Генерируй токены для новых shoots
   - ✅ Проверяй токены перед доступом
   - ✅ Используй `.env` для credentials
   - ✅ Валидируй user input

2. **Документируй**
   - ✅ Обновляй CHANGELOG.md
   - ✅ Пиши понятные commit messages
   - ✅ Комментируй сложную логику
   - ✅ Обновляй README при изменении features

3. **Тестируй**
   - ✅ Запускай `npm run build` перед commit
   - ✅ Проверяй в браузере критичные изменения
   - ✅ Тестируй на мобильных (если UI)

---

## 🔐 Security Checklist

При работе с любым кодом, проверь:

```
[ ] Токены генерируются криптографически безопасно?
[ ] Shoot pages требуют валидный токен?
[ ] PIN hash в .env, не в коде?
[ ] Нет hardcoded credentials?
[ ] URL sanitization работает?
[ ] XSS защита на месте?
[ ] Rate limiting активен?
```

---

## 📁 Структура проекта (quick reference)

```
clixy/
├── components/          # React компоненты
│   ├── Landing.tsx     # Публичный лендинг
│   ├── Dashboard.tsx   # Список съемок (PIN)
│   ├── AdminDashboard.tsx  # CRUD операции (PIN)
│   ├── ShootDetails.tsx    # Детали съемки (Token)
│   └── PinProtection.tsx   # PIN gate
├── utils/
│   ├── tokenUtils.ts   # 🔐 Token generation
│   ├── pinSecurity.ts  # 🔐 PIN + rate limiting
│   └── validation.ts   # Input validation
├── services/
│   └── shootService.ts # Supabase CRUD
├── types.ts            # TypeScript types
├── App.tsx             # Router + маршруты
└── constants.ts        # Mock data
```

**Маршруты:**
```
Публичные:
  / → Landing
  /gift-card → Gift cards
  /shoot/:id?token=xxx → Shoot details (ТРЕБУЕТ TOKEN!)

Защищенные PIN:
  /dashboard → Shoots list
  /admin → CRUD dashboard
  /admin/create → New shoot
  /admin/edit/:id → Edit shoot
```

---

## 🛠️ Common Tasks

### Создание нового компонента

```typescript
// 1. Создай файл в components/
// 2. Используй FC type
// 3. Соблюдай design system

import React from 'react';

interface MyComponentProps {
  // props
}

export const MyComponent: React.FC<MyComponentProps> = ({ props }) => {
  return (
    <div className="bg-[#D8D9CF] border-2 border-[#141413]">
      {/* content */}
    </div>
  );
};
```

### Добавление нового поля в Shoot

```typescript
// 1. Обнови types.ts
interface Shoot {
  // existing fields...
  newField: string; // Add here with comment
}

// 2. Обнови ShootForm.tsx (initial state)
const [formData, setFormData] = useState<Shoot>({
  // ...
  newField: '', // Default value
});

// 3. Добавь в форму (JSX)
// 4. Обнови constants.ts (MOCK_SHOOTS)
// 5. Обнови CHANGELOG.md
```

### Работа с безопасностью

```typescript
// Генерация токена
import { generateSecureToken } from '../utils/tokenUtils';
const token = generateSecureToken(); // 32 chars

// Проверка токена
import { isValidTokenFormat } from '../utils/tokenUtils';
if (!isValidTokenFormat(token)) {
  // Invalid format
}

// PIN hashing
node scripts/hashPin.cjs YOUR_PIN
# Copy hash to .env.local
```

---

## 🎨 Design System Guidelines

### Цвета (используй ТОЛЬКО эти)
```css
bg-[#D8D9CF]  /* Background - warm gray */
text-[#141413] /* Primary text - near black */
text-[#9E9E98] /* Secondary text - mid gray */
bg-[#F0F0EB]  /* Accent - light beige */
```

### Typography
```css
/* Headers */
text-2xl md:text-4xl font-bold uppercase tracking-tight

/* Labels */
text-xs uppercase tracking-widest font-bold

/* Body */
text-sm md:text-base font-light
```

### Shadows (brutalist style)
```css
shadow-[8px_8px_0px_0px_rgba(20,20,19,1)]
hover:shadow-[12px_12px_0px_0px_rgba(20,20,19,1)]
```

### Borders
```css
border-2 border-[#141413]
```

---

## 💬 Communication Style

### С пользователем:
- Говори на русском (пользователь русскоязычный)
- Будь конкретным и кратким
- Объясняй что делаешь и почему
- Спрашивай если что-то непонятно
- Не используй эмодзи без необходимости

### В коде:
- Комментарии на английском
- Commit messages на английском
- Переменные/функции на английском
- Документация может быть на русском

---

## 📝 Commit Message Format

```
<type>: <short description>

<detailed description if needed>

Changes:
- List of specific changes
- File modifications
- New features

Related: Issue #X (if applicable)
```

**Types:**
- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - документация
- `style:` - форматирование, стили
- `refactor:` - рефакторинг кода
- `test:` - тесты
- `chore:` - обновление зависимостей и т.п.

**Примеры:**
```
feat: Add token-based security for shoot pages
fix: Update handleShare to include access token
docs: Add CHANGELOG.md and PROJECT_STATUS.md
```

---

## 🔄 Changelog Update Format

При обновлении `CHANGELOG.md`:

```markdown
## [YYYY-MM-DD] - Brief Title

### 🎯 What changed

**Problem:**
- Describe the issue/requirement

**Solution:**
- What was implemented

#### Changes in code:

**1. File path (line numbers)**
- What changed
- Why it matters

**2. Another file**
- Changes

#### Security/Testing:

✅ List security improvements
✅ Testing notes

#### Documentation:

- Updated files

**Author:** AI Development Team
**Status:** ✅ Completed / ⏳ In Progress
```

---

## 🧪 Testing Checklist

Перед commit:

```bash
# 1. Build успешен?
export PATH="$HOME/.nvm/versions/node/v22.19.0/bin:$PATH"
npm run build

# 2. Нет TypeScript ошибок?
# (проверится в build)

# 3. Критичные функции работают?
# - PIN login
# - Token validation
# - Shoot creation
# - Copy link

# 4. Документация обновлена?
# - CHANGELOG.md
# - PROJECT_STATUS.md (если нужно)

# 5. Commit message понятен?
```

---

## 📞 When to Ask User

### ВСЕГДА спрашивай:
- Изменения в архитектуре
- Удаление существующего функционала
- Изменения в безопасности
- Изменения в design system
- Новые зависимости (npm install)
- Deployment в production

### Можешь делать сам:
- Bug fixes (очевидные)
- Документация
- Code formatting
- Мелкие UI tweaks (если просили)
- Tests (когда добавим)

---

## 🎓 Learning Resources

**Если не знаешь как что-то сделать:**

1. Посмотри существующий код:
   - `components/` - примеры компонентов
   - `utils/` - утилиты
   - `types.ts` - типы данных

2. Проверь документацию:
   - `README.md` - technical docs
   - `SECURITY_TESTING.md` - security testing

3. Спроси пользователя!

---

## 🐛 Known Issues & Gotchas

### Node Version
```bash
# Default node (v12) НЕ РАБОТАЕТ с vite!
# Нужен Node v22+
export PATH="$HOME/.nvm/versions/node/v22.19.0/bin:$PATH"
```

### Build Warnings
```bash
# "(!) Some chunks are larger than 500 kB"
# Это нормально, в будущем сделаем code-splitting
```

### HashRouter
```bash
# Используем HashRouter для GitHub Pages
# URLs вида: https://site.com/#/dashboard
# НЕ: https://site.com/dashboard
```

---

## 🎯 Success Criteria

Задача выполнена успешно если:

✅ `npm run build` проходит без ошибок
✅ Безопасность не нарушена
✅ CHANGELOG.md обновлен
✅ Код протестирован
✅ Commit создан с понятным сообщением
✅ Пользователь доволен результатом

---

## 💡 Tips & Tricks

1. **Используй существующие паттерны** - посмотри как реализованы похожие компоненты
2. **Не over-engineer** - простое решение лучше сложного
3. **Security first** - если сомневаешься, делай безопаснее
4. **Документируй сразу** - не откладывай на потом
5. **Тестируй в браузере** - особенно изменения в UI
6. **Git commits часто** - лучше 5 мелких чем 1 огромный

---

**Remember:**
- Ты работаешь над PRODUCTION проектом
- Настоящие люди используют этот портал
- Безопасность клиентских данных - приоритет #1
- Когда сомневаешься - СПРОСИ!

Good luck! 🚀
